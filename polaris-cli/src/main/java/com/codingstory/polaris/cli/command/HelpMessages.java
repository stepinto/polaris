package com.codingstory.polaris.cli.command;

public interface HelpMessages {
    public static final String INDEX = "  -i, --index=<dir>        the index directory, default: ./index\n";
    public static final String SERVER = "  -s, --server=<ip:port>   the search server to request from, default: null\n";
}
