form(ng-submit='search()')
  input.input-xlarge.search-query(type='text', ng-model='query')
  button.btn.search-button(type='submit') Search
div
  p Results: {{count}}
  p Latency: {{latency}}
  ul
    li(ng-repeat='hit in results')
      a(ng-href="{{hit.url}}") {{hit.project + hit.path}}
      pre {{hit.summary}}
